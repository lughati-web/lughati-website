/* ============================================================
   Lughati website — the ONE place for values that are not known yet.
   Leave a value empty ("") and the site keeps its honest placeholder.
   Fill it in and every page picks it up automatically.
   ============================================================ */
window.LUGHATI_SITE = {
  /* Google Play listing URL, e.g. "https://play.google.com/store/apps/details?id=com.lughati.app"
     Set only after the listing is live — until then buttons read "coming soon". */
  playUrl: "",

  /* Public contact / privacy email (the same address published in Play Console). */
  contactEmail: "lughatiapplication@gmail.com",

  /* Final public URL of this website, without a trailing slash (used for canonical + Open Graph). */
  siteUrl: "https://lughati-web.github.io/lughati-website"
};
